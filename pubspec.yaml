name: mason_logger_extension
description: A utility extension for Mason Logger
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  mason: ^0.1.0
  qr: ^3.0.1

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0
